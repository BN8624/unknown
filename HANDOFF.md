# HANDOFF

## 현재 목표

골드를 모아 공격력을 강화하고 적을 더 빨리 잡는 작은 성장 재미를 확인한다. (TASK_002)

## 지금까지 완료

- TASK_001: 자동전투 루프 (자동 전진 → 적 등장 → 자동 상호 공격 → 처치 → 반복), 임시 도형·체력 바·타격 표현·용병 사망 안전장치.
- 아이폰 테스트용 웹(HTML5) 내보내기 + Tailscale HTTPS 노출.
- TASK_002: 적 처치 시 골드 +5, 골드 표시, 공격력 강화 버튼(첫 비용 20, +2, 다음 비용 ×1.4), 현재/강화 후 공격력·비용 표시, 강화로 처치 타격 수 감소.

## 현재 작업

없음. TASK_002 구현·헤드리스 검증 완료. 사용자 플레이 확인 대기.

## 다음 액션

> 사용자가 직접 플레이해 "골드 모아 공격력 올리고 더 빨리 잡는 과정이 작은 만족감을 주는지" 확인한다. 확인 후에야 다음 성장(경험치·레벨업 또는 체력·방어력 강화)으로 넘어간다.

## 실행 방법

방법 A — 에디터로 실행 (권장, 화면 확인용).

```
"C:\Users\USER\godot-engine\Godot_v4.7-stable_win64.exe" --path "C:\Users\USER\unknown"
```

에디터가 열리면 F5(실행)로 전투 화면을 본다. 또는 다음으로 바로 실행한다.

```
"C:\Users\USER\godot-engine\Godot_v4.7-stable_win64.exe" --path "C:\Users\USER\unknown" Battle.tscn
```

방법 B — 자동 검증 (화면 없이 로그만).

```
"C:\Users\USER\godot-engine\Godot_v4.7-stable_win64_console.exe" --headless --path "C:\Users\USER\unknown" -- --verify
```

## 검증 상태

- 헤드리스 자동 검증(--verify): 골드 처치당 +5, 20골드에서 강화(공격력 10→12, 비용 ×1.4), 공격력 16에서 처치 타격 3→2로 감소 PASS, 종료 코드 0, 경고/오류 0건.
- 시각적 타격감·강화 버튼 조작감·성장 만족감은 미검증. 사용자 플레이 판단 영역.

## 남은 위험

- 임시 도형·임시 타격 표현이라 "강해지는 맛"은 직접 봐야 판단 가능.
- 용병 HP가 전투 사이 회복되지 않아 약 12처치마다 사망→전체 회복·리셋된다. 루프는 안 깨지지만 성장 체감을 해칠 수 있음. 회복 추가는 범위 밖이라 보류(`context-notes.md`).
- 강화 버튼/수치 균형(처음 강화까지 4처치, 체감 차이는 공격력 16부터)이 적절한지는 플레이로 확인. 수치는 `Battle.gd` 상단 상수에서 조정 가능.
