# HANDOFF

## 현재 목표

최소 자동전투 루프를 화면으로 확인한다. (TASK_001)

## 지금까지 완료

- Godot 4.7 최소 프로젝트 생성 (세로형 540×960).
- 용병 자동 전진 → 적 등장 → 전진 정지 → 자동 상호 공격 → 적 처치 → 다시 전진 → 다음 적 등장 루프 구현.
- 임시 도형(ColorRect), 체력 바, 타격 표현(전진 모션·피격 밝아짐·체력 바 감소), 용병 사망 시 자동 회복·재시작 처리.
- 결정적 전투(무작위 없음).

## 현재 작업

없음. TASK_001 구현·검증 완료.

## 다음 액션

> 사용자가 Godot 에디터에서 직접 실행해 전투 속도·타격 표현이 보기 좋은지 확인한다. 확인 후에야 골드 획득 구현 등 다음 단계로 넘어간다. (사용자 플레이 확인 전까지 골드/경험치/강화/특성을 이어서 구현하지 않는다.)

## 실행 방법

방법 A — 에디터로 실행 (권장, 화면 확인용).

```
"C:\Users\USER\godot-engine\Godot_v4.7-stable_win64.exe" --path "C:\Users\USER\unknown"
```

에디터가 열리면 F5(실행)로 전투 화면을 본다. 또는 다음으로 바로 실행한다.

```
"C:\Users\USER\godot-engine\Godot_v4.7-stable_win64.exe" --path "C:\Users\USER\unknown" Battle.tscn
```

방법 B — 자동 검증 (화면 없이 로그만).

```
"C:\Users\USER\godot-engine\Godot_v4.7-stable_win64_console.exe" --headless --path "C:\Users\USER\unknown" -- --verify
```

## 검증 상태

- 헤드리스 자동 검증: 적 6마리 연속 처치, 루프 안 깨짐, 용병 생존(HP 60/100), 종료 코드 0, 경고/오류 0건. (시간 가속 모드)
- 시각적 타격감·전진 자연스러움은 미검증. 사용자 플레이 판단 영역.

## 남은 위험

- 임시 도형·임시 타격 표현이라 "공격이 맞는 느낌"은 직접 봐야 판단 가능.
- 전진 속도/공격 간격이 보기에 빠른지 느린지는 화면 확인 필요. 수치는 `Battle.gd` 상단 상수에서 한 번에 조정 가능.
